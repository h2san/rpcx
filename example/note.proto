syntax = "proto3";
package note;


message AppListReq
{
    string name = 1;
    int64 id = 2;
}

message AppListResp
{
    string username = 1;
    int64 age = 2;
}

service Note
{
    rpc GetAppList(AppListReq) returns(AppListResp);
    rpc AppList(AppListReq) returns(AppListResp);
}
